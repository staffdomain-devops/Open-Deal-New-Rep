---
phase: 01-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/utils.py
  - requirements.txt
autonomous: true
requirements:
  - SCAF-01
  - SCAF-02

must_haves:
  truths:
    - "scripts/utils.py is byte-for-byte identical to the Inbound source"
    - "requirements.txt lists all five dependencies with correct minimum version pins"
    - "Importing utils in a Python session exposes write_dlq, HS_RETRY_KWARGS, REQ_RETRY_KWARGS, ANTHROPIC_RETRY_KWARGS, safe_truncate"
  artifacts:
    - path: "scripts/utils.py"
      provides: "Retry/DLQ helpers shared by every pipeline script"
      exports: ["write_dlq", "HS_RETRY_KWARGS", "REQ_RETRY_KWARGS", "ANTHROPIC_RETRY_KWARGS", "safe_truncate"]
    - path: "requirements.txt"
      provides: "Pinned dependency list for pip install"
      contains: "hubspot-api-client>=12.0.0"
  key_links:
    - from: "scripts/utils.py"
      to: "tenacity"
      via: "import at top of file"
      pattern: "from tenacity import"
    - from: "requirements.txt"
      to: "scripts/utils.py"
      via: "tenacity>=9.0.0 satisfies tenacity import"
      pattern: "tenacity>=9.0.0"
---

<objective>
Copy the shared utility module from the Inbound project unchanged, and create the requirements.txt listing all five pipeline dependencies.

Purpose: Every subsequent pipeline script imports from utils.py for retry/DLQ behaviour. Getting an identical copy now prevents divergence and satisfies SCAF-01. requirements.txt is needed before any script can be tested.

Output: scripts/utils.py (copied verbatim), requirements.txt (five pinned deps).
</objective>

<execution_context>
@C:\Users\irahfo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\irahfo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\ROADMAP.md
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\REQUIREMENTS.md

<interfaces>
<!-- Source file to copy verbatim — read it, then write it character-for-character -->
Source: C:\Users\irahfo\Outreach\Inbound\scripts\utils.py

Exports (must all be present in the copy):
  write_dlq(contact_id, contact_email, failed_step, error_message, retry_count) -> None
  HS_RETRY_KWARGS: dict  (wait=_hs_combined_wait, stop=attempt(6)|delay(60), reraise=True)
  REQ_RETRY_KWARGS: dict (same wait/stop as HS)
  ANTHROPIC_RETRY_KWARGS: dict (wait=_anthropic_combined_wait, stop=attempt(6)|delay(60), reraise=True)
  safe_truncate(text: str, max_chars: int) -> str

Internal helpers (private, must not be removed):
  _is_hubspot_transient(exc) -> bool
  _is_requests_transient(exc) -> bool
  _is_anthropic_transient(exc) -> bool
  RetryAfterWait   (class — Retry-After in seconds)
  HubSpotRetryAfterWait (class — Retry-After in milliseconds)
  _hs_combined_wait(retry_state) -> float
  _anthropic_combined_wait(retry_state) -> float

Imports in source:
  json, os, datetime (datetime, timezone)
  tenacity: RetryCallState, stop_after_attempt, stop_after_delay, wait_random_exponential

Source requirements.txt: C:\Users\irahfo\Outreach\Inbound\requirements.txt
  hubspot-api-client>=12.0.0
  requests>=2.31.0
  beautifulsoup4>=4.12.0
  anthropic>=0.30.0
  tenacity>=9.0.0
</interfaces>
</context>

## Phase Goal

**As a** pipeline developer, **I want to** have the shared utilities and dependency list in place, **so that** every subsequent script can import retry/DLQ helpers and be installed with a single pip command.

<tasks>

<task type="auto">
  <name>Task 1: Copy utils.py from Inbound verbatim (SCAF-01)</name>
  <files>scripts/utils.py</files>
  <read_first>
    - C:\Users\irahfo\Outreach\Inbound\scripts\utils.py (source of truth — copy this exactly)
  </read_first>
  <action>
    Create the directory scripts/ if it does not exist. Read C:\Users\irahfo\Outreach\Inbound\scripts\utils.py in full, then write its contents to scripts/utils.py at the project root (C:\Users\irahfo\Outreach\Open Deal, New Rep\scripts\utils.py) without any modification. Do NOT add, remove, or change any line. Do NOT add a module docstring. Do NOT rename anything. The decision to reuse Inbound utils.py unchanged is locked (Decisions Log 2026-08-21: "Reuse Inbound utils.py unchanged — same retry/DLQ patterns; no divergence").
  </action>
  <verify>
    <automated>python -c "import sys; sys.path.insert(0, 'scripts'); import utils; assert callable(utils.write_dlq); assert callable(utils.safe_truncate); assert isinstance(utils.HS_RETRY_KWARGS, dict); assert 'wait' in utils.HS_RETRY_KWARGS; print('utils.py OK')"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/utils.py exists at C:\Users\irahfo\Outreach\Open Deal, New Rep\scripts\utils.py
    - File contains the exact string "def write_dlq(contact_id, contact_email, failed_step, error_message, retry_count):" on a single line
    - File contains the exact string "class HubSpotRetryAfterWait:" (millisecond Retry-After interpreter)
    - File contains "HS_RETRY_KWARGS", "REQ_RETRY_KWARGS", "ANTHROPIC_RETRY_KWARGS" as module-level dict assignments
    - File contains "def safe_truncate(text: str, max_chars: int) -> str:"
    - Python import succeeds: `import utils` with sys.path pointing to scripts/
    - Line count matches source: both files have 123 lines
  </acceptance_criteria>
  <done>scripts/utils.py is present and importable; all five public names resolve without error.</done>
</task>

<task type="auto">
  <name>Task 2: Create requirements.txt with five pinned dependencies (SCAF-02)</name>
  <files>requirements.txt</files>
  <read_first>
    - C:\Users\irahfo\Outreach\Inbound\requirements.txt (reference — exact version pins to match)
    - C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\REQUIREMENTS.md (SCAF-02 lists exact versions)
  </read_first>
  <action>
    Create requirements.txt at the project root (C:\Users\irahfo\Outreach\Open Deal, New Rep\requirements.txt) with exactly these five lines, one per line, no blank lines, no comments:

    hubspot-api-client>=12.0.0
    requests>=2.31.0
    beautifulsoup4>=4.12.0
    anthropic>=0.30.0
    tenacity>=9.0.0

    Version pins are LOCKED in SCAF-02 — do not upgrade, downgrade, or change the specifier (>= is required, == is wrong). Do not add any other package.
  </action>
  <verify>
    <automated>python -c "lines = open('requirements.txt').read().strip().splitlines(); expected = ['hubspot-api-client>=12.0.0','requests>=2.31.0','beautifulsoup4>=4.12.0','anthropic>=0.30.0','tenacity>=9.0.0']; assert lines == expected, f'Mismatch: {lines}'; print('requirements.txt OK')"</automated>
  </verify>
  <acceptance_criteria>
    - requirements.txt exists at project root
    - Exactly 5 lines, no blank lines, no trailing whitespace issues that would break pip
    - Line 1 is exactly "hubspot-api-client>=12.0.0"
    - Line 2 is exactly "requests>=2.31.0"
    - Line 3 is exactly "beautifulsoup4>=4.12.0"
    - Line 4 is exactly "anthropic>=0.30.0"
    - Line 5 is exactly "tenacity>=9.0.0"
    - All specifiers use ">=" not "==" or "~="
  </acceptance_criteria>
  <done>requirements.txt present with all five deps at correct minimum versions; pip install -r requirements.txt would succeed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → scripts/utils.py | Read from Inbound path, written to project path — copy fidelity risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | scripts/utils.py copy | mitigate | Line-count and named-symbol assertions in verify step confirm byte fidelity |
| T-01-02 | Tampering | requirements.txt version pins | mitigate | Exact-match assertion on all five lines; ">=" specifier enforced |
</threat_model>

<verification>
Run both automated verify commands in sequence from the project root (C:\Users\irahfo\Outreach\Open Deal, New Rep\):

1. python -c "import sys; sys.path.insert(0, 'scripts'); import utils; assert callable(utils.write_dlq); print('utils OK')"
2. python -c "lines = open('requirements.txt').read().strip().splitlines(); assert len(lines) == 5; assert lines[0] == 'hubspot-api-client>=12.0.0'; print('requirements OK')"
</verification>

<success_criteria>
- scripts/utils.py is present and importable with all five public names (write_dlq, safe_truncate, HS_RETRY_KWARGS, REQ_RETRY_KWARGS, ANTHROPIC_RETRY_KWARGS)
- requirements.txt has exactly five lines matching SCAF-02 verbatim
- No modifications to utils.py vs Inbound source
</success_criteria>

<output>
After completion, create .planning/phases/01-scaffolding/01-01-SUMMARY.md
</output>
