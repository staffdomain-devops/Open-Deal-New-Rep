---
phase: 01-scaffolding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - config/vertical_routing.py
  - config/close_bank.py
autonomous: true
requirements:
  - SCAF-03
  - SCAF-04

must_haves:
  truths:
    - "route('accounting firm') returns a VerticalRoute with case_study='SJM Accountants (accounting firm)'"
    - "route('') or route(None) returns the default row (Bells Pure Ice)"
    - "route() is case-insensitive: route('IT SERVICES') matches the IT row"
    - "assign_close(35, 'CFO', 'partial') returns (3, <option 3 text>) — heavy touch overrides C-suite"
    - "assign_close(5, 'CEO', 'partial') returns (4, <option 4 text>)"
    - "assign_close(5, 'Manager', 'exact') returns (5, <option 5 text>)"
    - "assign_close(5, 'Manager', 'partial') rotates 1→2→1→2 across successive calls"
  artifacts:
    - path: "config/vertical_routing.py"
      provides: "14-row routing table with route() function"
      exports: ["route", "VerticalRoute", "ROUTING_TABLE"]
    - path: "config/close_bank.py"
      provides: "5 locked close options and assign_close() function"
      exports: ["assign_close", "CLOSE_BANK", "_close_counter"]
  key_links:
    - from: "config/vertical_routing.py"
      to: "config/close_bank.py"
      via: "industry_match_strength parameter in assign_close comes from route() result"
      pattern: "industry_match_strength"
    - from: "config/vertical_routing.py"
      to: "scripts/exclude_and_route.py (Phase 3)"
      via: "route(industry) called in routing stage"
      pattern: "from config.vertical_routing import route"
---

<objective>
Implement the two deterministic routing/close config modules that every later phase consumes: the 14-row vertical routing table and the 5-option close bank with its assignment logic.

Purpose: Phase 3 (Exclusion & Routing) calls route() and assign_close() directly. Getting them right now, with full test coverage, means Phase 3 can treat them as trusted primitives.

Output: config/vertical_routing.py (route function + 14 rows), config/close_bank.py (assign_close + 5 locked closes).
</objective>

<execution_context>
@C:\Users\irahfo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\irahfo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\ROADMAP.md
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\REQUIREMENTS.md

<interfaces>
<!-- Routing table source: SD_Reengagement_LaneA_Build_Spec.md §4, table reproduced here -->

Row | Match tokens (any substring, case-insensitive) | case_study | email3_url | email4_url
1   | accounting, bookkeep, tax                      | SJM Accountants (accounting firm)       | https://www.staffdomain.com/accounting-finance/             | https://www.staffdomain.com/build-your-team/
2   | staffing, recruit, human resources, executive search | Cox Purtell (recruitment)          | https://www.staffdomain.com/solutions/recruitment/          | https://www.staffdomain.com/build-your-team/hr-recruitment/
3   | it services, information technology, computer software, software, computer network, cyber, telecommunications, technology | Systemnet (Sydney MSP) | https://www.staffdomain.com/technology/ | https://www.staffdomain.com/build-your-team/
4   | construction, civil engineering, building, architecture, engineering, joinery, design | Carrera by Design (construction/joinery) | https://www.staffdomain.com/construction-engineering/ | https://www.staffdomain.com/build-your-team/construction-support/
5   | hospital, health, medical, care, wellness       | Verus (healthcare)                      | https://www.staffdomain.com/health-care/                    | https://www.staffdomain.com/build-your-team/
6   | law, legal                                      | Elias Gates (legal)                     | https://www.staffdomain.com/professional-services/          | https://www.staffdomain.com/build-your-team/
7   | marketing, advertising, public relations, graphic design, media | Capital-E (marketing and events) | https://www.staffdomain.com/professional-services/ | https://www.staffdomain.com/build-your-team/sales-marketing/
8   | real estate, property, leasing                  | Capital-E (finance team)                | https://www.staffdomain.com/real-estate/                    | https://www.staffdomain.com/build-your-team/
9   | financial services, insurance, investment, banking, capital markets | Durst Industries (accounting) | https://www.staffdomain.com/accounting-finance/ | https://www.staffdomain.com/solutions/
10  | consulting, professional training, business services | Interlinked (professional services) | https://www.staffdomain.com/professional-services/          | https://www.staffdomain.com/solutions/
11  | logistics, transportation, supply chain, warehousing, maritime | Liftango (logistics)    | https://www.staffdomain.com/industries/                     | https://www.staffdomain.com/build-your-team/
12  | retail, e-commerce, consumer, hospitality, food, leisure | EatFirst (customer service)    | https://www.staffdomain.com/build-your-team/customer-service/ | https://www.staffdomain.com/build-your-team/
13  | manufactur, machinery, mining, industrial, wholesale, automotive, utilities, oil and gas, chemical, printing | Bells Pure Ice (manufacturing) | https://www.staffdomain.com/industries/ | https://www.staffdomain.com/build-your-team/
14  | (default — no match or empty)                   | Bells Pure Ice (manufacturing)          | https://www.staffdomain.com/industries/                     | https://www.staffdomain.com/build-your-team/

Close bank (LOCKED — JP's exact wording, spec §5.3):
  1: "Just getting across it all at the moment, and wanted to introduce myself. I'm sure our paths will cross at some stage."
  2: "Still working my way through the history at the moment, so this is a hello more than anything. No doubt we'll speak properly down the track."
  3: "Mostly wanted to make sure the handover didn't happen quietly without you knowing. I expect we'll cross paths before long."
  4: "Nothing more to this than putting a name to the new face on your account. We'll catch up properly at some stage down the track, I'm sure."
  5: "Getting my bearings on everything at the moment more than anything else. Once I'm across it all properly we can have more of a chat when the time suits."

assign_close priority (spec §3.7 + §5.3):
  1st (highest): option 3 if touches >= 30
  2nd: option 4 if jobtitle contains any of CEO, MD, COO, CFO, Partner, Principal, Director (case-insensitive; "clearly top-of-business" — substring match is acceptable)
  3rd: option 5 if industry_match_strength == "exact"
  fallback: rotate 1 → 2 → 1 → 2 using a module-level counter (_close_counter)
</interfaces>
</context>

## Phase Goal

**As a** pipeline developer, **I want to** have the routing table and close bank encoded as importable Python modules, **so that** Phase 3 can deterministically route any contact to the right case study URLs and close text without hardcoding logic in the orchestration layer.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement config/vertical_routing.py — 14-row routing table (SCAF-03)</name>
  <files>config/vertical_routing.py, config/__init__.py</files>
  <read_first>
    - C:\Users\irahfo\Outreach\Open Deal, New Rep\SD_Reengagement_LaneA_Build_Spec.md (§4 — routing table, all 14 rows and the hard URL rules)
    - C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\REQUIREMENTS.md (SCAF-03)
  </read_first>
  <behavior>
    - route("accounting") -> VerticalRoute(case_study="SJM Accountants (accounting firm)", email3_url="https://www.staffdomain.com/accounting-finance/", email4_url="https://www.staffdomain.com/build-your-team/")
    - route("Bookkeeping Services") -> same SJM row (case-insensitive "bookkeep" substring)
    - route("IT Services") -> VerticalRoute with case_study="Systemnet (Sydney MSP)"
    - route("Computer Software") -> same Systemnet row
    - route("Real Estate Agency") -> VerticalRoute(case_study="Capital-E (finance team)", email3_url="https://www.staffdomain.com/real-estate/")
    - route("Manufacturing") -> VerticalRoute(case_study="Bells Pure Ice (manufacturing)")
    - route("") -> default row (Bells Pure Ice)
    - route(None) -> default row (Bells Pure Ice) without raising
    - route("Logistics & Supply Chain") -> VerticalRoute(case_study="Liftango (logistics)")
    - First-hit-wins: "Staffing & Recruitment" matches row 2 (staffing), not row 10 (business services)
  </behavior>
  <action>
    Create config/__init__.py (empty) and config/vertical_routing.py.

    Define a VerticalRoute dataclass (or namedtuple) with fields: case_study (str), email3_url (str), email4_url (str).

    Define ROUTING_TABLE as a list of dicts, each with keys "tokens" (list of str), "case_study", "email3_url", "email4_url". Populate all 13 named rows from spec §4 using the exact strings from the interfaces block above — do not alter URLs or case study names.

    The default row (row 14) uses an empty "tokens" list and must appear last.

    Implement route(industry: str) -> VerticalRoute:
      - Normalize: industry.lower().strip() if industry else ""
      - Iterate ROUTING_TABLE top-to-bottom; for each row, check if ANY token in row["tokens"] is a substring of the normalized industry string
      - First match wins — return that VerticalRoute immediately
      - If no token row matches (including empty/None industry), return the default row (last entry, tokens=[])
      - The function must never raise on None or empty string input

    Do NOT add the "industry_match_strength" field to VerticalRoute — that is computed by assign_close's caller, not by route(). Do not add logging or print statements.
  </action>
  <verify>
    <automated>python -c "
import sys; sys.path.insert(0, '.')
from config.vertical_routing import route, VerticalRoute
r = route('accounting'); assert r.case_study == 'SJM Accountants (accounting firm)', r
r = route('IT Services'); assert r.case_study == 'Systemnet (Sydney MSP)', r
r = route('Real Estate'); assert r.case_study == 'Capital-E (finance team)', r
r = route(''); assert r.case_study == 'Bells Pure Ice (manufacturing)', r
r = route(None); assert r.case_study == 'Bells Pure Ice (manufacturing)', r
r = route('Staffing & Recruitment'); assert r.case_study == 'Cox Purtell (recruitment)', r
print('vertical_routing OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - config/vertical_routing.py exists; config/__init__.py exists (may be empty)
    - VerticalRoute has exactly three fields: case_study, email3_url, email4_url
    - ROUTING_TABLE has exactly 14 entries (13 named rows + 1 default)
    - route("") and route(None) both return case_study="Bells Pure Ice (manufacturing)" without raising
    - route("Bookkeeping Services") returns case_study="SJM Accountants (accounting firm)" (case-insensitive substring "bookkeep")
    - route("IT Services") returns case_study="Systemnet (Sydney MSP)"
    - route("Real Estate") returns email3_url="https://www.staffdomain.com/real-estate/"
    - route("Staffing & Recruitment") returns case_study="Cox Purtell (recruitment)" (row 2 wins over row 10)
    - No URL in ROUTING_TABLE contains the substring "offshore", "outsourc", or "bpo" (hard URL rule from spec §4)
    - All 6 test assertions in the automated verify command pass
  </acceptance_criteria>
  <done>route() returns correct VerticalRoute for all 14 table rows; None/empty falls back to default; case-insensitivity confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement config/close_bank.py — 5-option close bank with assignment logic (SCAF-04)</name>
  <files>config/close_bank.py</files>
  <read_first>
    - C:\Users\irahfo\Outreach\Open Deal, New Rep\SD_Reengagement_LaneA_Build_Spec.md (§5.3 — the five close options verbatim, and §3.7 for assignment rules)
    - C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\REQUIREMENTS.md (SCAF-04)
  </read_first>
  <behavior>
    - assign_close(35, "Manager", "partial") -> (3, "Mostly wanted to make sure the handover didn't happen quietly without you knowing. I expect we'll cross paths before long.")
    - assign_close(5, "CEO", "partial") -> (4, "Nothing more to this than putting a name to the new face on your account. We'll catch up properly at some stage down the track, I'm sure.")
    - assign_close(5, "Managing Director", "partial") -> (4, ...) — "MD" as substring of "Managing Director" matches
    - assign_close(5, "Manager", "exact") -> (5, "Getting my bearings on everything at the moment more than anything else. Once I'm across it all properly we can have more of a chat when the time suits.")
    - assign_close(5, "Manager", "partial") on first call -> (1, "Just getting across it all at the moment, and wanted to introduce myself. I'm sure our paths will cross at some stage.")
    - assign_close(5, "Manager", "partial") on second call -> (2, "Still working my way through the history at the moment...")
    - assign_close(5, "Manager", "partial") on third call -> (1, ...) [counter wraps]
    - option 3 (touches>=30) overrides option 4 (C-suite): assign_close(35, "CEO", "partial") -> (3, ...)
  </behavior>
  <action>
    Create config/close_bank.py.

    Define CLOSE_BANK as a dict mapping int option number (1–5) to the EXACT close text from spec §5.3. Copy the wording character-for-character from the spec — these are JP's locked lines. Do not paraphrase, do not "improve" punctuation.

    The five options verbatim (from spec §5.3):
      1: "Just getting across it all at the moment, and wanted to introduce myself. I'm sure our paths will cross at some stage."
      2: "Still working my way through the history at the moment, so this is a hello more than anything. No doubt we'll speak properly down the track."
      3: "Mostly wanted to make sure the handover didn't happen quietly without you knowing. I expect we'll cross paths before long."
      4: "Nothing more to this than putting a name to the new face on your account. We'll catch up properly at some stage down the track, I'm sure."
      5: "Getting my bearings on everything at the moment more than anything else. Once I'm across it all properly we can have more of a chat when the time suits."

    Define _close_counter as a module-level list containing a single integer [0] (mutable, so the function can mutate it without `global`).

    Implement assign_close(touches: int, jobtitle: str, industry_match_strength: str) -> tuple[int, str]:
      Priority order (highest first):
        1. If touches >= 30: return (3, CLOSE_BANK[3])
        2. Elif any of ["CEO","MD","COO","CFO","Partner","Principal","Director"] is a substring of jobtitle (case-insensitive): return (4, CLOSE_BANK[4])
        3. Elif industry_match_strength == "exact": return (5, CLOSE_BANK[5])
        4. Else: use _close_counter[0] to pick option 1 or 2 (0 -> option 1, 1 -> option 2); increment counter mod 2; return (option_number, CLOSE_BANK[option_number])

    C-suite check note: "MD" as a standalone substring will match "Managing Director" — this is intentional per spec ("MD" is in the jobtitle token list).

    Do not add logging or print statements. The function must be stateless except for the rotation counter.
  </action>
  <verify>
    <automated>python -c "
import sys; sys.path.insert(0, '.')
from config.close_bank import assign_close, CLOSE_BANK, _close_counter

# Reset counter before test
_close_counter[0] = 0

# Option 3 — heavy touch (overrides C-suite)
n, t = assign_close(35, 'CEO', 'partial')
assert n == 3, f'Expected 3 got {n}'

# Option 4 — C-suite
n, t = assign_close(5, 'CEO', 'partial')
assert n == 4, f'Expected 4 got {n}'

# Option 4 — MD substring
n, t = assign_close(5, 'Managing Director', 'partial')
assert n == 4, f'Expected 4 for MD got {n}'

# Option 5 — exact match
n, t = assign_close(5, 'Manager', 'exact')
assert n == 5, f'Expected 5 got {n}'

# Option 1/2 rotation
_close_counter[0] = 0
n1, _ = assign_close(5, 'Manager', 'partial')
n2, _ = assign_close(5, 'Manager', 'partial')
n3, _ = assign_close(5, 'Manager', 'partial')
assert n1 == 1, f'Expected 1 got {n1}'
assert n2 == 2, f'Expected 2 got {n2}'
assert n3 == 1, f'Expected 1 (wrap) got {n3}'

# Verify close text is verbatim
assert CLOSE_BANK[1].startswith('Just getting across it all at the moment')
assert CLOSE_BANK[4].startswith('Nothing more to this than putting a name')
print('close_bank OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - config/close_bank.py exists
    - CLOSE_BANK has exactly 5 keys (1, 2, 3, 4, 5) with string values
    - CLOSE_BANK[1] starts with "Just getting across it all at the moment, and wanted to introduce myself."
    - CLOSE_BANK[2] starts with "Still working my way through the history at the moment"
    - CLOSE_BANK[3] starts with "Mostly wanted to make sure the handover didn't happen quietly"
    - CLOSE_BANK[4] starts with "Nothing more to this than putting a name to the new face on your account."
    - CLOSE_BANK[5] starts with "Getting my bearings on everything at the moment"
    - assign_close(35, "CEO", "partial") returns option 3 (touches>=30 wins over C-suite)
    - assign_close(5, "CEO", "partial") returns option 4
    - assign_close(5, "Manager", "exact") returns option 5
    - assign_close(5, "Manager", "partial") rotates 1→2→1 across three consecutive calls (with counter reset to 0)
    - All assertions in the automated verify command pass
  </acceptance_criteria>
  <done>assign_close() returns the correct (option_number, close_text) tuple for all four assignment branches; close text matches spec §5.3 verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contact record → route(industry) | Industry string from HubSpot property — untrusted free text |
| brief assembler → assign_close() | touches/jobtitle/match_strength computed upstream — trusted pipeline data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | CLOSE_BANK wording | mitigate | Acceptance criteria assert exact start-of-string for all 5 options; any edit breaks the assertion |
| T-02-02 | Tampering | ROUTING_TABLE URL values | mitigate | No URL in table may contain "offshore", "outsourc", or "bpo" — asserted in acceptance criteria |
| T-02-03 | Information Disclosure | INTERNAL notes leaking via routing | accept | Routing module uses only the industry string; it never receives note content |
</threat_model>

<verification>
Run from project root (C:\Users\irahfo\Outreach\Open Deal, New Rep\):

1. python -c "from config.vertical_routing import route; r=route('accounting'); assert r.case_study=='SJM Accountants (accounting firm)'; print('routing OK')"
2. python -c "from config.close_bank import assign_close, _close_counter; _close_counter[0]=0; n,t=assign_close(35,'CEO','partial'); assert n==3; print('close bank OK')"
</verification>

<success_criteria>
- route() correctly maps all 14 rows; default row fires on empty/None industry
- Case-insensitive substring matching confirmed for at least 6 distinct industry inputs
- assign_close() returns option 3 for touches>=30, option 4 for C-suite, option 5 for exact match, rotates 1/2 otherwise
- All five CLOSE_BANK strings match spec §5.3 verbatim (start-of-string assertions)
</success_criteria>

<output>
After completion, create .planning/phases/01-scaffolding/01-02-SUMMARY.md
</output>
