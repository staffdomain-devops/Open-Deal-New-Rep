---
phase: 01-scaffolding
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - config/system_prompt.py
autonomous: true
requirements:
  - SCAF-05

must_haves:
  truths:
    - "SYSTEM_PROMPT constant is present and non-empty in config/system_prompt.py"
    - "SYSTEM_PROMPT contains the exact phrase 'You write re-engagement emails for Staff Domain' as its opening"
    - "SYSTEM_PROMPT's OUTPUT block contains all 8 keys: e1, e2, e3, e4, e5, call1, call2, pin"
    - "SYSTEM_PROMPT contains the v1.1 internal-notes paragraph beginning 'call1, call2 and pin are INTERNAL notes'"
    - "SYSTEM_PROMPT contains the amended INTERNAL - NEVER REFERENCE rule with 'in any email' qualification"
    - "SYSTEM_PROMPT contains the v1.1 geography block beginning 'The brief resolves COUNTRY to AU, NZ, US or UK'"
    - "get_system_prompt() returns the same string as SYSTEM_PROMPT"
  artifacts:
    - path: "config/system_prompt.py"
      provides: "Verbatim v1.0+v1.1 system prompt as module constant"
      exports: ["SYSTEM_PROMPT", "get_system_prompt"]
  key_links:
    - from: "config/system_prompt.py"
      to: "scripts/generate_campaign.py (Phase 4)"
      via: "from config.system_prompt import get_system_prompt; passed with cache_control ephemeral"
      pattern: "get_system_prompt"
---

<objective>
Encode the verbatim system prompt (v1.0 base from spec §5.2, with the three v1.1 surgical edits from the amendment) as a Python module constant.

Purpose: Phase 4 imports SYSTEM_PROMPT directly and passes it to the Anthropic API with cache_control ephemeral. The prompt must be verbatim — every voice failure in testing traced to paraphrasing a rule. This plan isolates the highest-fidelity task in the phase.

Output: config/system_prompt.py with SYSTEM_PROMPT constant and get_system_prompt() accessor.
</objective>

<execution_context>
@C:\Users\irahfo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\irahfo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\ROADMAP.md
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\REQUIREMENTS.md

<interfaces>
<!-- Two source files must BOTH be read before writing anything -->
<!-- Source 1: v1.0 base prompt — SD_Reengagement_LaneA_Build_Spec.md §5.2 (the fenced block) -->
<!-- Source 2: v1.1 amendments — New SDR - Deal old deal outreach.md §AMENDS §5.2 -->

v1.1 delta — THREE surgical edits to the v1.0 base:

EDIT (a): Replace the OUTPUT block's JSON schema.
  v1.0 OUTPUT block ends with:
    {"e1":{"subject":"...","body":"..."},
     "e2":{"subject":"...","body":"..."},
     "e3":{"subject":"...","body":"..."},
     "e4":{"subject":"...","body":"..."},
     "e5":{"subject":"...","body":"..."}}
    Body text uses \n\n between paragraphs. ...
  Replace the JSON schema line(s) with the 8-key schema:
    {"e1":{"subject":"...","body":"..."},
     "e2":{"subject":"...","body":"..."},
     "e3":{"subject":"...","body":"..."},
     "e4":{"subject":"...","body":"..."},
     "e5":{"subject":"...","body":"..."},
     "call1":{"body":"..."},
     "call2":{"body":"..."},
     "pin":{"body":"..."}}
  Then APPEND immediately after the schema (before "Body text uses..."):
    call1, call2 and pin are INTERNAL notes for the salesperson, never seen by the
    recipient. Write them in plain telegraphic English following the exact labelled
    formats given in the brief. They are the one place the record's real outcome
    must appear, including anything marked INTERNAL - NEVER REFERENCE: state it
    plainly under HOW IT ENDED, and when you do, you must also write a DO NOT SAY
    line telling the salesperson what can never be voiced and what the on-record
    phrasing is. The five emails remain absolutely forbidden from carrying that
    material. The salesperson has never spoken to this contact: the previous rep
    has left, and everything the salesperson knows comes from these notes, so
    HISTORY must name the previous rep and anchor when contact last happened.
    Call notes: 100 words maximum each. Pin note: 130 words maximum. No greeting,
    no sign-off, no persuasion — these are briefings, not emails.

EDIT (b): Amend the INTERNAL - NEVER REFERENCE rule in HARD MECHANICAL RULES.
  v1.0 text: "Never quote or paraphrase anything the brief marks INTERNAL - NEVER REFERENCE."
  Replace with: "Never quote or paraphrase anything the brief marks INTERNAL - NEVER REFERENCE in any email; in call1, call2 and pin it must appear, per the OUTPUT rules."

EDIT (c): Replace the two geography sentences in HARD MECHANICAL RULES.
  v1.0 text (two sentences to replace):
    "Australian English spelling. For Australian records the Australian context is
    assumed: never write \"Australian businesses like yours\" or \"Aussie\". For
    non-Australian records (the brief will say), nothing may assume Australia."
  Replace with:
    "- The brief resolves COUNTRY to AU, NZ, US or UK. AU: Australian context is
      assumed, never stated. NZ, US, UK: nothing may assume Australia; market
      observations stay neutral. US records: avoid dialect-marked spellings
      entirely (write around them; say \"two weeks\", never \"fortnight\").
      Australian English spelling everywhere else."

Everything else in the v1.0 prompt is UNCHANGED. The prompt structure, voice rules,
calibration examples, hard mechanical rules (other than the two edits above), email
definitions, and variety instruction all carry forward verbatim.
</interfaces>
</context>

## Phase Goal

**As a** pipeline developer, **I want to** have the system prompt encoded verbatim in a Python module, **so that** the generation stage can import and cache it without any risk of paraphrasing or drift from the spec.

<tasks>

<task type="auto">
  <name>Task 1: Assemble and write config/system_prompt.py with v1.0 + v1.1 prompt (SCAF-05)</name>
  <files>config/system_prompt.py</files>
  <read_first>
    - C:\Users\irahfo\Outreach\Open Deal, New Rep\SD_Reengagement_LaneA_Build_Spec.md (§5.2 — the complete fenced system prompt, verbatim source)
    - C:\Users\irahfo\Outreach\Open Deal, New Rep\New SDR - Deal old deal outreach.md (§AMENDS §5.2 — three surgical edits)
  </read_first>
  <action>
    Read the COMPLETE system prompt from SD_Reengagement_LaneA_Build_Spec.md §5.2 (the block between the fences). Copy it verbatim. Do NOT paraphrase, condense, reorder, or "improve" any sentence.

    Then apply exactly three edits from the amendment file (New SDR - Deal old deal outreach.md, §AMENDS §5.2):

    EDIT (a): In the OUTPUT section, replace the 5-key JSON schema with the 8-key schema shown in the interfaces block. Immediately after the new schema (before "Body text uses \n\n..."), insert the "call1, call2 and pin are INTERNAL notes..." paragraph verbatim from the amendment.

    EDIT (b): In HARD MECHANICAL RULES, find the sentence "Never quote or paraphrase anything the brief marks INTERNAL - NEVER REFERENCE." and replace it with "Never quote or paraphrase anything the brief marks INTERNAL - NEVER REFERENCE in any email; in call1, call2 and pin it must appear, per the OUTPUT rules."

    EDIT (c): In HARD MECHANICAL RULES, find the Australian English / geography sentences and replace them with the Lane B v2.1 COUNTRY block from the amendment (the "- The brief resolves COUNTRY to AU, NZ, US or UK..." bullet, verbatim).

    After applying the three edits, create config/system_prompt.py with:
      - A module-level string constant named SYSTEM_PROMPT assigned the full edited prompt (use a triple-quoted string)
      - A function get_system_prompt() -> str that returns SYSTEM_PROMPT

    The file must not import anything. It must not have any logic other than the constant and the one-line accessor function. No print statements, no __main__ block.

    CRITICAL: Do not invent, add, or remove any sentence that is not in the v1.0 spec or the v1.1 amendment. If you are unsure whether a word is in the original, re-read the source file.
  </action>
  <verify>
    <automated>python -c "
import sys; sys.path.insert(0, '.')
from config.system_prompt import SYSTEM_PROMPT, get_system_prompt

# Basic presence
assert SYSTEM_PROMPT, 'SYSTEM_PROMPT is empty'
assert get_system_prompt() == SYSTEM_PROMPT

# v1.0 opening line
assert SYSTEM_PROMPT.startswith('You write re-engagement emails for Staff Domain'), 'Wrong opening'

# v1.1 edit (a): 8-key schema present
assert '\"call1\":{\"body\":' in SYSTEM_PROMPT or 'call1' in SYSTEM_PROMPT, 'call1 key missing from OUTPUT'
assert '\"call2\":{\"body\":' in SYSTEM_PROMPT or 'call2' in SYSTEM_PROMPT, 'call2 key missing'
assert '\"pin\":{\"body\":' in SYSTEM_PROMPT or 'pin' in SYSTEM_PROMPT, 'pin key missing'
assert 'call1, call2 and pin are INTERNAL notes for the salesperson' in SYSTEM_PROMPT, 'v1.1 internal notes paragraph missing'

# v1.1 edit (b): amended INTERNAL rule
assert 'in any email' in SYSTEM_PROMPT, 'v1.1 edit (b): amended INTERNAL rule missing'

# v1.1 edit (c): COUNTRY block
assert 'The brief resolves COUNTRY to AU, NZ, US or UK' in SYSTEM_PROMPT, 'v1.1 edit (c): COUNTRY block missing'

# v1.0 voice rules still present
assert 'THE CORE RULE: casualness comes from SENTENCE CONSTRUCTION' in SYSTEM_PROMPT
assert 'No em dashes anywhere' in SYSTEM_PROMPT
assert 'EMAIL 1, Day 1' in SYSTEM_PROMPT
assert 'EMAIL 5, Day 21' in SYSTEM_PROMPT

print('system_prompt OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - config/system_prompt.py exists
    - SYSTEM_PROMPT is a non-empty string constant
    - get_system_prompt() returns the identical string as SYSTEM_PROMPT (identity or equality)
    - SYSTEM_PROMPT begins with "You write re-engagement emails for Staff Domain"
    - SYSTEM_PROMPT contains "THE SITUATION." section verbatim
    - SYSTEM_PROMPT contains "VOICE." section with "THE CORE RULE: casualness comes from SENTENCE CONSTRUCTION"
    - SYSTEM_PROMPT contains the full banned-words list including "mob, have a crack, been burnt"
    - SYSTEM_PROMPT contains "HARD MECHANICAL RULES." with "No em dashes anywhere."
    - SYSTEM_PROMPT contains EMAIL 1 through EMAIL 5 definitions
    - SYSTEM_PROMPT contains "VARIETY ACROSS THE SEQUENCE." section
    - v1.1 edit (a): SYSTEM_PROMPT contains "call1, call2 and pin are INTERNAL notes for the salesperson, never seen by the recipient"
    - v1.1 edit (a): SYSTEM_PROMPT contains all 8 keys in the OUTPUT JSON schema: e1, e2, e3, e4, e5, call1, call2, pin
    - v1.1 edit (b): SYSTEM_PROMPT contains "INTERNAL - NEVER REFERENCE in any email" (the amended rule)
    - v1.1 edit (c): SYSTEM_PROMPT contains "The brief resolves COUNTRY to AU, NZ, US or UK"
    - v1.0 max_tokens reference "2000" does NOT appear; the spec raised this to 3000 in v1.1 (the prompt itself does not set tokens — that is API config — but the word-count ceiling "45 to 110 words" for emails must still be present)
    - File has no imports, no __main__ block, and no logic other than SYSTEM_PROMPT constant and get_system_prompt() function
    - All 10 assertions in the automated verify command pass
  </acceptance_criteria>
  <done>config/system_prompt.py present; SYSTEM_PROMPT is the verbatim v1.0 prompt with all three v1.1 surgical edits applied; get_system_prompt() accessor works.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec files → SYSTEM_PROMPT string | Human-authored spec → Python constant; fidelity risk is paraphrasing or missed edits |
| config/system_prompt.py → Anthropic API | Prompt sent as system message; any deviation changes model behaviour |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | SYSTEM_PROMPT content | mitigate | 10 specific string assertions in verify step; any paraphrase breaks at least one |
| T-03-02 | Information Disclosure | INTERNAL tags leaking to emails | mitigate | v1.1 edit (b) must be present — assert "in any email" appears in the INTERNAL rule |
| T-03-03 | Tampering | v1.1 edits omitted | mitigate | Three dedicated assertions (edit a, b, c) in verify command — all three must pass |
</threat_model>

<verification>
Run from project root (C:\Users\irahfo\Outreach\Open Deal, New Rep\):

python -c "
from config.system_prompt import SYSTEM_PROMPT, get_system_prompt
assert SYSTEM_PROMPT.startswith('You write re-engagement emails for Staff Domain')
assert 'call1, call2 and pin are INTERNAL notes for the salesperson' in SYSTEM_PROMPT
assert 'in any email' in SYSTEM_PROMPT
assert 'The brief resolves COUNTRY to AU, NZ, US or UK' in SYSTEM_PROMPT
assert get_system_prompt() == SYSTEM_PROMPT
print('PASS')
"
</verification>

<success_criteria>
- config/system_prompt.py present with SYSTEM_PROMPT constant and get_system_prompt() function
- All v1.0 sections present verbatim (VOICE, HARD MECHANICAL RULES, five email definitions, VARIETY, OUTPUT)
- All three v1.1 surgical edits applied: 8-key schema + internal-notes paragraph, amended INTERNAL rule, COUNTRY block
- No paraphrasing anywhere — source text copied character-for-character
</success_criteria>

<output>
After completion, create .planning/phases/01-scaffolding/01-03-SUMMARY.md
</output>
