---
phase: 05-lint-assembly
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/lint.py
  - scripts/assemble_bodies.py
autonomous: true
requirements:
  - LINT-04
  - ASSEM-01
  - ASSEM-02
  - ASSEM-03

must_haves:
  truths:
    - "_regenerate_contact(contact_id, brief) imports _call_realtime, parse_output, write_generated from generate_campaign; calls _call_realtime(brief['brief_text']); returns new parsed dict or raises"
    - "main() reads passing_ids.json; writes DLQ sentinel at startup; for each contact: lint → if hard fail → regenerate once → re-lint → if still fail → write_dlq + print stderr; if pass → write lint_passing_ids.json entry"
    - "main() writes lint_passing_ids.json (list of contact_ids that passed lint) to RUNNER_TEMP after the loop"
    - "Every 10th passing contact (counter % 10 == 0) is written to review_sample.json with reason 'Nth sample'"
    - "Every soft-warning contact is written to review_sample.json with the warning list as reasons"
    - "assemble_bodies.py reads lint_passing_ids.json; for each cid loads generated_{cid}.json; appends placeholder to e2 and e5; writes assembled_{cid}.json"
    - "E2 placeholder: append '\\n[Insert {case_study} case study link here]' after the body (after [Rep first name] sign-off)"
    - "E5 placeholder: append '\\n[Insert rep booking link here]' after the body"
    - "E1, E3, E4 bodies written unchanged to assembled_{cid}.json"
    - "assemble_bodies.py writes assembled_{cid}.json to RUNNER_TEMP; schema mirrors generated_{cid}.json with e1–e5 bodies/subjects + call1/call2/pin bodies amended"
  artifacts:
    - path: "scripts/lint.py"
      provides: "Full lint script with regeneration wrapper and main() loop"
      min_lines: 280
      contains: "_regenerate_contact"
    - path: "scripts/assemble_bodies.py"
      provides: "Body assembly script — appends link placeholders to e2 and e5"
      min_lines: 60
  key_links:
    - from: "lint.py main() hard-fail branch"
      to: "_regenerate_contact()"
      via: "if hard_failures: _regenerate_contact(cid, brief)"
      pattern: "_regenerate_contact"
    - from: "lint.py main()"
      to: "lint_passing_ids.json"
      via: "json.dump(lint_passing, ...) after loop"
      pattern: "lint_passing_ids\\.json"
    - from: "assemble_bodies.py main()"
      to: "lint_passing_ids.json"
      via: "json.load on RUNNER_TEMP/lint_passing_ids.json"
      pattern: "lint_passing_ids\\.json"
    - from: "assemble_bodies.py"
      to: "assembled_{cid}.json"
      via: "json.dump with amended e2/e5 bodies"
      pattern: "assembled_.*\\.json"
---

<objective>
Extend lint.py with the regeneration wrapper (_regenerate_contact) and main() loop, then
create assemble_bodies.py. Together these complete Phase 5: contacts that pass lint are
written to lint_passing_ids.json, a review_sample.json is accumulated, and assemble_bodies.py
appends the link placeholders to E2 and E5 for each passing contact.

Output: scripts/lint.py (amended — _regenerate_contact + main() added),
        scripts/assemble_bodies.py (new)
</objective>

<execution_context>
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\workflows\execute-plan.md
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\templates\summary.md
</execution_context>

<context>
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\PROJECT.md
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\ROADMAP.md
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\STATE.md
@C:\Users\irahfo\Outreach\Open Deal, New Rep\.planning\phases\05-lint-assembly\05-01-SUMMARY.md

<interfaces>
From scripts/lint.py (after Plan 05-01 — read the full file):
  run_lint(generated, brief, contact_record, seen_subjects) -> tuple[list, list]
  _write_to_review_sample(contact_id, generated, reasons, out_path) -> None
  EMAIL_KEYS, CALL_KEYS, URL_REGEX (module-level constants)

From scripts/generate_campaign.py (read the file):
  _call_realtime(brief_text: str) -> anthropic.types.Message
  parse_output(response, contact_id: str) -> dict
  write_generated(contact_id: str, parsed: dict, brief: dict) -> None
  MaxTokensError, OutputParseError (exception classes)

From scripts/utils.py (read the file):
  write_dlq(contact_id, contact_email, failed_step, error_message, retry_count) -> None

File paths in RUNNER_TEMP:
  passing_ids.json — list of contact IDs that cleared exclusion (written by Phase 3)
  generated_{id}.json — Phase 4 output per passing contact
  brief_{id}.json — Phase 3 brief per passing contact
  contact_{id}.json — Phase 2 record per contact
  lint_passing_ids.json — WRITTEN by lint.py main(); consumed by assemble_bodies.py
  review_sample.json — accumulated by _write_to_review_sample() calls
  assembled_{id}.json — WRITTEN by assemble_bodies.py per passing contact

generated_{id}.json / assembled_{id}.json schema:
  {
    "contact_id": str,
    "e1": {"subject": str, "body": str},
    "e2": {"subject": str, "body": str},
    "e3": {"subject": str, "body": str},
    "e4": {"subject": str, "body": str},
    "e5": {"subject": str, "body": str},
    "call1": {"body": str},
    "call2": {"body": str},
    "pin": {"body": str},
    "case_study": str,
    "close_option": int,
    "close_text": str,
  }

assembled_{id}.json: same schema but e2.body and e5.body have placeholder lines appended.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add _regenerate_contact() and main() to scripts/lint.py</name>
  <files>scripts/lint.py</files>
  <read_first>
    - scripts/lint.py — read full file as it exists after Plan 05-01; identify last line to append after
    - scripts/generate_campaign.py — confirm _call_realtime, parse_output, write_generated signatures; confirm MaxTokensError and OutputParseError class names
    - scripts/utils.py — confirm write_dlq signature
    - scripts/exclude_and_route.py lines 320–340 — main() DLQ sentinel pattern and loop structure to replicate
  </read_first>
  <action>
    Append to scripts/lint.py after _write_to_review_sample():

    Imports (add to the existing import block at top of file — READ the file first to see what's already there):
      Add: from generate_campaign import (_call_realtime, parse_output, write_generated,
                                         MaxTokensError, OutputParseError)
      Add: from utils import write_dlq
    These must be appended to the imports section AT THE TOP of the file, not in the middle.
    Read the current import block (lines 1–15 approx) before editing.

    _regenerate_contact function (add before main()):

    def _regenerate_contact(contact_id: str, brief: dict) -> dict:
      response = _call_realtime(brief["brief_text"])
      parsed = parse_output(response, contact_id)
      write_generated(contact_id, parsed, brief)
      return parsed

    main() function:

    def main():
      ids_path = os.path.join(RUNNER_TEMP, "passing_ids.json")
      with open(ids_path) as f:
        contact_ids = json.load(f)

      review_path = os.path.join(RUNNER_TEMP, "review_sample.json")
      write_dlq("batch", "", "startup", "sentinel", 0)

      seen_subjects: dict = {}
      lint_passing: list = []
      pass_counter = 0  # counts contacts that passed lint (for every-Nth sampling)

      for cid in contact_ids:
        gen_path = os.path.join(RUNNER_TEMP, f"generated_{cid}.json")
        brief_path = os.path.join(RUNNER_TEMP, f"brief_{cid}.json")
        record_path = os.path.join(RUNNER_TEMP, f"contact_{cid}.json")

        # Skip if any required file is absent
        if not os.path.exists(gen_path) or not os.path.exists(brief_path):
          continue
        if not os.path.exists(record_path):
          continue

        with open(gen_path, encoding="utf-8") as f:
          generated = json.load(f)
        with open(brief_path, encoding="utf-8") as f:
          brief = json.load(f)
        with open(record_path, encoding="utf-8") as f:
          contact_record = json.load(f)

        try:
          hard_failures, soft_warnings = run_lint(generated, brief, contact_record, seen_subjects)

          if hard_failures:
            print(f"LINT FAIL (attempt 1) {cid}: {hard_failures[0]}", file=sys.stderr)
            # Regenerate once
            try:
              generated = _regenerate_contact(cid, brief)
              hard_failures, soft_warnings = run_lint(generated, brief, contact_record, seen_subjects)
            except (MaxTokensError, OutputParseError) as exc:
              write_dlq(cid, "", "lint_regenerate", str(exc), 0)
              print(f"REGEN FAIL {cid}: {exc}", file=sys.stderr)
              continue

          if hard_failures:
            write_dlq(cid, "", "lint_hard_fail", "; ".join(hard_failures), 0)
            print(f"LINT FAIL (attempt 2, flagged) {cid}: {hard_failures[0]}", file=sys.stderr)
            continue

          # Contact passed lint
          lint_passing.append(cid)
          pass_counter += 1

          # Soft warning → review sample
          if soft_warnings:
            _write_to_review_sample(cid, generated, soft_warnings, review_path)
          # Every-Nth sample (N=10)
          elif pass_counter % 10 == 0:
            _write_to_review_sample(cid, generated, ["Nth sample"], review_path)

        except Exception as exc:
          write_dlq(cid, "", "lint_unexpected", str(exc), 0)
          print(f"ERROR lint {cid}: {exc}", file=sys.stderr)

      # Write lint_passing_ids.json
      passing_path = os.path.join(RUNNER_TEMP, "lint_passing_ids.json")
      with open(passing_path, "w", encoding="utf-8") as f:
        json.dump(lint_passing, f, indent=2)
      print(f"Lint complete: {len(lint_passing)}/{len(contact_ids)} passed. Written {passing_path}")

    if __name__ == "__main__": main()
  </action>
  <verify>
    python -c "import ast; ast.parse(open('scripts/lint.py').read()); print('syntax OK')"
    grep -n "def _regenerate_contact\|def main\|lint_passing_ids\|pass_counter" scripts/lint.py
    grep -c "from generate_campaign import\|from utils import write_dlq" scripts/lint.py
  </verify>
  <acceptance_criteria>
    - ast.parse passes with no SyntaxError
    - grep shows "def _regenerate_contact" present
    - grep shows "def main" present
    - grep shows "lint_passing_ids.json" present (at least 2 lines: path assignment + json.dump)
    - grep shows "from generate_campaign import" present
    - grep shows "from utils import write_dlq" present
    - grep shows "pass_counter % 10" present (every-Nth sampling)
    - grep shows "write_dlq" called at startup (sentinel) and on lint_hard_fail: grep -c "write_dlq" returns >= 3
    - imports are at the top of the file (not inside function bodies): grep -n "from generate_campaign import" shows line number < 20
  </acceptance_criteria>
  <done>lint.py extended with _regenerate_contact(), main(), and correct import additions; syntax check passes</done>
</task>

<task type="auto">
  <name>Task 2: Create scripts/assemble_bodies.py</name>
  <files>scripts/assemble_bodies.py</files>
  <read_first>
    - SD_Reengagement_LaneA_Build_Spec.md §6 — exact placeholder text and placement rules
    - scripts/generate_campaign.py — for module docstring pattern + imports pattern
    - scripts/lint.py — confirm lint_passing_ids.json is the input list (written by lint.py main())
  </read_first>
  <action>
    Create scripts/assemble_bodies.py.

    Module docstring: one-line summary + Usage line (python scripts/assemble_bodies.py).

    Imports: json, os, sys (stdlib); from utils import write_dlq.

    Module-level constants:
      RUNNER_TEMP = os.environ.get("RUNNER_TEMP", ".")

    Helper function:
      def _append_placeholder(body: str, placeholder: str) -> str:
        Return body + "\n" + placeholder
        (Spec §6: append on a new line at the very bottom of the body, after the sign-off)

    main() function:
      1. Load lint_passing_ids.json from RUNNER_TEMP
      2. Write DLQ sentinel at startup: write_dlq("batch", "", "startup", "sentinel", 0)
      3. For each cid in contact_ids:
         - Load generated_{cid}.json
         - Build assembled dict: copy all keys from generated unchanged
         - Amend e2.body: assembled["e2"]["body"] = _append_placeholder(
             generated["e2"]["body"],
             f"[Insert {generated.get('case_study', 'case study')} case study link here]"
           )
         - Amend e5.body: assembled["e5"]["body"] = _append_placeholder(
             generated["e5"]["body"],
             "[Insert rep booking link here]"
           )
         - E1, E3, E4 bodies: copied unchanged from generated (no modification)
         - Write assembled_{cid}.json to RUNNER_TEMP
         - On exception: write_dlq(cid, "", "assemble_bodies", str(exc), 0); print to stderr; continue
      4. Print summary: f"Assembly complete: {assembled_count}/{total} contacts assembled."

    if __name__ == "__main__": main()
  </action>
  <verify>
    python -c "import ast; ast.parse(open('scripts/assemble_bodies.py').read()); print('syntax OK')"
    grep -n "lint_passing_ids\|assembled_\|case study link\|booking link\|_append_placeholder" scripts/assemble_bodies.py
    grep -c "write_dlq" scripts/assemble_bodies.py
  </verify>
  <acceptance_criteria>
    - ast.parse passes with no SyntaxError
    - grep shows "lint_passing_ids.json" present (reads the Phase 5 passing list)
    - grep shows "assembled_{" present (writes assembled_{cid}.json)
    - grep shows "case study link here" present (ASSEM-01 placeholder)
    - grep shows "booking link here" present (ASSEM-02 placeholder)
    - grep shows "_append_placeholder" defined as a function
    - grep -c "write_dlq" returns >= 2 (sentinel + per-contact error)
    - E3 and E4 bodies are NOT amended: grep -c "e3.*append\|e4.*append\|append.*e3\|append.*e4" returns 0
    - case_study from generated dict is interpolated into e2 placeholder: grep -n "case_study" shows at least 1 match
    - DLQ sentinel at startup: grep -n "startup.*sentinel\|sentinel" shows match
  </acceptance_criteria>
  <done>assemble_bodies.py reads lint_passing_ids.json, appends placeholders to E2 and E5 bodies, writes assembled_{cid}.json per contact; E1/E3/E4 unchanged</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lint.py → generate_campaign.py | _regenerate_contact imports and calls _call_realtime directly; shares the module-level client and SYSTEM_MESSAGE (and thus ANTHROPIC_API_KEY) |
| lint.py → assemble_bodies.py | via lint_passing_ids.json (written by lint.py, read by assemble_bodies.py) |
| assemble_bodies.py → HubSpot (Phase 6) | assembled_{id}.json is the handoff; Phase 6 read from this file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Denial of Service | Regeneration doubles API calls per contact on hard fail | accept | Max one regeneration per contact; failed contacts go to DLQ and are skipped; no infinite loop |
| T-05-06 | Tampering | case_study value from generated dict interpolated into e2 placeholder | accept | case_study is carry-through from brief (deterministic routing table lookup); not user-supplied free text; cannot inject bracket characters that would trigger Phase 6 bracket guard |
| T-05-07 | Information Disclosure | lint_passing_ids.json reveals which contacts were excluded by lint | accept | RUNNER_TEMP only; same sensitivity as exclusion_report.json; not uploaded unless failure |
| T-05-08 | Repudiation | Contacts that fail both lint attempts silently dropped | mitigate | write_dlq called with lint_hard_fail for every double-failed contact; never silently discarded |
</threat_model>

<verification>
Run all checks sequentially:

1. python -c "import ast; ast.parse(open('scripts/lint.py').read()); print('lint.py OK')"
2. python -c "import ast; ast.parse(open('scripts/assemble_bodies.py').read()); print('assemble_bodies.py OK')"
3. grep -c "def check_h" scripts/lint.py  — expect 18
4. grep -c "def check_w" scripts/lint.py  — expect 5
5. grep -c "def _regenerate_contact\|def main\b" scripts/lint.py  — expect 2
6. grep -n "lint_passing_ids.json" scripts/lint.py  — expect >= 2 lines
7. grep -n "lint_passing_ids.json" scripts/assemble_bodies.py  — expect >= 1
8. grep -n "assembled_" scripts/assemble_bodies.py  — expect >= 2 (path assignment + write)
9. grep -n "case study link here" scripts/assemble_bodies.py  — expect 1
10. grep -n "booking link here" scripts/assemble_bodies.py  — expect 1
</verification>

<success_criteria>
1. lint.py syntax-checks clean; total ≥ 280 lines
2. _regenerate_contact imports from generate_campaign and calls _call_realtime + parse_output + write_generated
3. main() reads passing_ids.json; writes DLQ sentinel; runs lint loop with regeneration on hard fail; writes lint_passing_ids.json
4. Every 10th passing contact lands in review_sample.json; every soft-warning contact lands in review_sample.json
5. assemble_bodies.py syntax-checks clean; reads lint_passing_ids.json
6. E2 body gets "\n[Insert {case_study} case study link here]" appended
7. E5 body gets "\n[Insert rep booking link here]" appended
8. E1, E3, E4 bodies unchanged in assembled_{id}.json
9. assembled_{id}.json written to RUNNER_TEMP per passing contact
</success_criteria>

<output>
After completion, create .planning/phases/05-lint-assembly/05-02-SUMMARY.md using the summary template.
Update STATE.md and ROADMAP.md to reflect Phase 5 planned.
</output>
