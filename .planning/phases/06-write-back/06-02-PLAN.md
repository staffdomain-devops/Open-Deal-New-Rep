---
phase: 06-write-back
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - scripts/write_hubspot.py
autonomous: true
requirements:
  - WRITE-04

must_haves:
  truths:
    - "A HubSpot note is created for each contact using the pin body from assembled_{id}.json"
    - "Each note is associated with its contact immediately after creation"
    - "Pin attempt sets hs_pinned_engagement_id on the contact; failure appends to manual_pin_list.json and does not raise"
    - "manual_pin_list.json is a list of {contact_id, note_id} dicts in RUNNER_TEMP"
    - "Note creation and pin failure are both non-fatal — contact processing continues on exception"
  artifacts:
    - path: "scripts/write_hubspot.py"
      provides: "Note creation, note-contact association, pin attempt with manual fallback"
      exports:
        - "_create_note"
        - "_pin_note"
  key_links:
    - from: "_create_note"
      to: "client.crm.objects.notes.basic_api.create()"
      via: "@retry(**HS_RETRY_KWARGS)"
      pattern: "notes\\.basic_api\\.create"
    - from: "_create_note"
      to: "client.crm.objects.notes.associations_api.create()"
      via: "post-create association call"
      pattern: "associations_api\\.create"
    - from: "_pin_note"
      to: "manual_pin_list.json"
      via: "json append on ApiException"
      pattern: "manual_pin_list\\.json"
---

<objective>
Amend scripts/write_hubspot.py (created in Plan 06-01) to add note creation and pinning per contact.

Purpose: Each contact's pin body (assembled["pin"]["body"]) is written as a HubSpot note, associated with the contact, then pinned via hs_pinned_engagement_id. If pinning fails (API does not support it or returns an error), the contact_id and note_id are appended to manual_pin_list.json for a manual pin pass. The pipeline continues regardless.

Output: scripts/write_hubspot.py amended — _create_note and _pin_note added; main() calls them per contact after the properties batch is accumulated.
</objective>

<execution_context>
@C:\Users\irahfo\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\irahfo\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@scripts/utils.py
@scripts/write_hubspot.py
@.planning/phases/06-write-back/06-01-SUMMARY.md
</context>

<interfaces>
<!-- State of write_hubspot.py after Plan 06-01 — executor MUST read the actual file before amending. -->

Existing functions (from 06-01):
  _check_property_schema(client)       — schema abort on startup
  _bracket_guard(assembled)            — raises ValueError on "[" in email fields
  _build_batch_input(contact_id, assembled) -> dict
  _write_properties_batch(client, batch_inputs)  — @retry, batch_api.update
  main()                               — load IDs, sentinel, schema check, batch loop, flush, summary

main() loop structure (from 06-01, for reference):
  for cid in contact_ids:
      load assembled_{cid}.json
      try:
          _bracket_guard(assembled)
          pending_batch.append(_build_batch_input(...))
          if len(pending_batch) == BATCH_SIZE: flush batch
      except Exception as exc:
          write_dlq(...)
          continue

Plan 06-02 amends the try block to add note creation + pin after batch accumulation.

HubSpot Notes API (CRM Objects):

    from hubspot.crm.objects.notes import SimplePublicObjectInputForCreate
    from hubspot.crm.associations import AssociationSpec

    # Create note
    note_input = SimplePublicObjectInputForCreate(
        properties={
            "hs_note_body": body,
            "hs_timestamp": datetime.now(timezone.utc).isoformat(),
        }
    )
    response = client.crm.objects.notes.basic_api.create(
        simple_public_object_input_for_create=note_input
    )
    note_id = response.id

    # Associate note with contact
    client.crm.objects.notes.associations_api.create(
        note_id=note_id,
        to_object_type="contacts",
        to_object_id=contact_id,
        association_type="note_to_contact",   # or numeric type_id=202
    )

HubSpot pin via contact update (from project_context):

    client.crm.contacts.basic_api.update(
        contact_id=contact_id,
        simple_public_object_input=SimplePublicObjectInput(
            properties={"hs_pinned_engagement_id": str(note_id)}
        )
    )

manual_pin_list.json format: list of {"contact_id": str, "note_id": str} written to RUNNER_TEMP.
Read-modify-write pattern (same as _write_to_review_sample in lint.py):
  load existing list if file exists, else start with []
  append new entry
  json.dump back to file
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add _create_note and _pin_note; amend main() to call them per contact</name>

  <files>scripts/write_hubspot.py</files>

  <read_first>
    - scripts/write_hubspot.py — read the full file as produced by Plan 06-01 before adding anything; confirm existing structure
    - scripts/utils.py — confirm HS_RETRY_KWARGS import is already present (it should be from 06-01)
    - scripts/lint.py — see _write_to_review_sample for read-modify-write pattern to replicate in _pin_note fallback
  </read_first>

  <action>
Amend scripts/write_hubspot.py. Do NOT rewrite the file from scratch — read the existing content and add to it. The 5 existing functions from Plan 06-01 must remain unchanged.

IMPORTS TO ADD at top of file (if not already present):

    from datetime import datetime, timezone
    from hubspot.crm.objects.notes import SimplePublicObjectInputForCreate
    from hubspot.crm.contacts import SimplePublicObjectInput   # for pin update

NEW FUNCTION: _create_note(client, contact_id: str, body: str) -> str
  - Decorated with @retry(**HS_RETRY_KWARGS)
  - Creates a note object:
      note_input = SimplePublicObjectInputForCreate(
          properties={
              "hs_note_body": body,
              "hs_timestamp": datetime.now(timezone.utc).isoformat(),
          }
      )
      response = client.crm.objects.notes.basic_api.create(
          simple_public_object_input_for_create=note_input
      )
      note_id = response.id
  - Associates the note with the contact:
      client.crm.objects.notes.associations_api.create(
          note_id=note_id,
          to_object_type="contacts",
          to_object_id=contact_id,
          association_type="note_to_contact",
      )
  - Returns note_id (str)

NEW FUNCTION: _pin_note(contact_id: str, note_id: str, client) -> bool
  - NOT decorated with @retry — pin failure must be caught and handled as a soft fallback, not retried
  - Attempts client.crm.contacts.basic_api.update with properties={"hs_pinned_engagement_id": str(note_id)}
      Use SimplePublicObjectInput(properties={...}) as the update payload
  - On success: returns True
  - On any Exception: appends {"contact_id": str(contact_id), "note_id": str(note_id)} to manual_pin_list.json in RUNNER_TEMP using read-modify-write (load existing list or [], append, json.dump); prints warning to stderr; returns False

AMEND main() loop — inside the existing per-contact try block, AFTER the pending_batch accumulation and flush logic, add:

    note_id = _create_note(client, str(cid), assembled["pin"]["body"])
    _pin_note(str(cid), note_id, client)

This means the try block now covers both property staging AND note creation. If note creation raises, the existing except block handles it (write_dlq + continue). The _pin_note call never raises — it handles its own exception internally.

The per-contact try block after amendment:

    try:
        _bracket_guard(assembled)
        pending_batch.append(_build_batch_input(str(cid), assembled))
        if len(pending_batch) == BATCH_SIZE:
            _write_properties_batch(client, pending_batch)
            written_count += len(pending_batch)
            pending_batch = []
        note_id = _create_note(client, str(cid), assembled["pin"]["body"])
        _pin_note(str(cid), note_id, client)
    except Exception as exc:
        write_dlq(cid, assembled.get("contact_id", str(cid)), "write_properties_or_note", str(exc), 0)
        error_count += 1
        print(f"ERROR {cid}: {exc}", file=sys.stderr)
        continue

Update the failed_step string in the except block from "write_properties" to "write_properties_or_note" to reflect that note creation is now also in scope.
  </action>

  <verify>
    <automated>cd '/c/Users/irahfo/Outreach/Open Deal, New Rep/scripts' && python -c "
import ast, sys
with open('write_hubspot.py') as f:
    src = f.read()
tree = ast.parse(src)
fns = {n.name for n in ast.walk(tree) if isinstance(n, ast.FunctionDef)}
required_new = {'_create_note', '_pin_note'}
required_old = {'_check_property_schema', '_bracket_guard', '_build_batch_input', '_write_properties_batch', 'main'}
missing = (required_new | required_old) - fns
if missing:
    print('MISSING functions:', missing)
    sys.exit(1)
if 'manual_pin_list.json' not in src:
    print('ERROR: manual_pin_list.json not referenced in source')
    sys.exit(1)
if 'hs_note_body' not in src:
    print('ERROR: hs_note_body not found — note creation may be incomplete')
    sys.exit(1)
print('AST check passed. All 7 required functions present, manual_pin_list.json and hs_note_body referenced.')
"</automated>
  </verify>

  <acceptance_criteria>
    - scripts/write_hubspot.py still parses cleanly after amendment (no syntax errors introduced)
    - All 7 functions present: the 5 from 06-01 plus _create_note and _pin_note
    - _create_note is decorated with @retry(**HS_RETRY_KWARGS); creates note with hs_note_body + hs_timestamp; associates with contact; returns note_id string
    - _pin_note is NOT decorated with @retry; catches all exceptions internally; appends to manual_pin_list.json using read-modify-write on failure; returns bool
    - main() loop calls _create_note then _pin_note for each contact inside the per-contact try block
    - manual_pin_list.json path constructed as os.path.join(RUNNER_TEMP, "manual_pin_list.json")
    - _pin_note failure does not raise and does not write to DLQ — it only writes to manual_pin_list.json and prints to stderr
    - The 5 functions from Plan 06-01 are unchanged (no logic modifications to _check_property_schema, _bracket_guard, _build_batch_input, _write_properties_batch, or the non-loop parts of main)
  </acceptance_criteria>

  <done>_create_note and _pin_note added; main() calls both per contact; manual_pin_list.json fallback implemented; file parses cleanly; all 7 functions present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| assembled["pin"]["body"] -> HubSpot Notes API | Note body sourced from generated output; no user-supplied input reaches the note |
| _pin_note -> HubSpot Contacts API | hs_pinned_engagement_id update; failure is non-fatal; fallback to manual_pin_list.json |
| _pin_note -> manual_pin_list.json | Read-modify-write; concurrent writes not a concern (single-process script) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | _bracket_guard (inherited) | mitigate | Bracket guard in 06-01 runs before note body is read; pin body is exempt and sourced directly from assembled["pin"]["body"] without modification |
| T-06-02 | Tampering | _check_property_schema (inherited) | mitigate | Schema check from 06-01 still runs at startup before any note creation |
| T-06-03 | Denial of Service | _create_note | mitigate | Wrapped in @retry(**HS_RETRY_KWARGS) for transient HubSpot errors; if all retries exhausted, exception is caught by main() try/except and written to DLQ |
| T-06-04 | Availability | _pin_note | accept | Pin API may not be supported; fallback is manual_pin_list.json; non-fatal; pilot verification will confirm whether pinning works before full-list run |
</threat_model>

<verification>
After execution:

1. File still parses: python -c "import ast; ast.parse(open('scripts/write_hubspot.py').read()); print('OK')"
2. All 7 functions present (see automated verify command above)
3. manual_pin_list.json referenced: grep "manual_pin_list" scripts/write_hubspot.py returns at least 1 line
4. hs_note_body present in note creation: grep "hs_note_body" scripts/write_hubspot.py returns at least 1 line
5. _pin_note has no @retry decorator: grep -A2 "def _pin_note" scripts/write_hubspot.py — first 2 lines after def must NOT contain "retry"
6. _create_note has @retry: grep -B2 "def _create_note" scripts/write_hubspot.py — line before def must contain "retry"
</verification>

<success_criteria>
- scripts/write_hubspot.py importable with all 7 functions defined
- _create_note creates a note with hs_note_body and hs_timestamp, associates it to the contact, and returns the note_id string
- _pin_note catches all exceptions; on failure writes {contact_id, note_id} to manual_pin_list.json in RUNNER_TEMP; never raises
- main() calls _create_note then _pin_note per contact inside the existing try block
- Full pipeline integration: running write_hubspot.py against assembled_{id}.json files writes 12 properties in batches, creates 1 note per contact, and attempts to pin each note
</success_criteria>

<output>
After completion, create .planning/phases/06-write-back/06-02-SUMMARY.md
</output>
